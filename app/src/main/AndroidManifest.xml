<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cste06.nstu.suvro.telemedicine">


    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />


            </intent-filter>
        </activity>

        <activity android:name=".UserActivity"></activity>
        <activity android:name=".DoctorActivity"></activity>
        <activity android:name=".ExtendedDoctorActivity"></activity>

        <activity android:name=".GenericActivity"></activity>
        <activity android:name=".MedicineForGenericActivity"></activity>
        <activity android:name=".ExtendedGenericActivity"></activity>

        <activity android:name=".MedicineActivity"></activity>
        <activity android:name=".ExtendedMedicineActivity"></activity>

        <activity android:name=".SearchActivity">

            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <meta-data android:name="android.app.searchable"
                android:resource="@xml/searchable" />

        </activity>
        <activity android:name=".ExtendedSearchActivity"></activity>
    </application>

</manifest>
